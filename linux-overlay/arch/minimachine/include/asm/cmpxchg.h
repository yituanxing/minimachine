#ifndef _ASM_MINIMACHINE_CMPXCHG_H
#define _ASM_MINIMACHINE_CMPXCHG_H

/* First Linux target is UP, so interrupt exclusion is a valid atomicity contract. */
#include <asm-generic/cmpxchg.h>

#endif /* _ASM_MINIMACHINE_CMPXCHG_H */
